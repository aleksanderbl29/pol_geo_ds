source("renv/activate.R")

if (interactive()) {
  source("~/.Rprofile")
}

library(targets)
library(tarchetypes)

# suppressMessages(
#   conflicted::conflicts_prefer(dplyr::filter,
#                                dplyr::lag)
# )
